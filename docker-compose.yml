services:
  database:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=postgres
    networks:
      - network
    volumes:
      - pgdata:/var/lib/postgresql/data
    hostname: postgres

  server:
    image: ruby
    ports:
      - 3000:3000
    volumes:
      - bundle:/usr/local/bundle
      - .:/app
    working_dir: /app
    command: bash -c "bundle install && ruby server.rb"
    networks:
      - network

  prepare_database:
    image: ruby
    volumes:
      - bundle:/usr/local/bundle
      - .:/app
    working_dir: /app/bin
    command: bash -c "bundle install && ruby import_from_csv_to_db.rb"
    networks:
      - network
    depends_on:
      - database

  test_database:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=test_db
    networks:
      - network
    volumes:
      - test_pgdata:/var/lib/postgresql/data
    hostname: test_postgres

  tests:
    image: ruby
    volumes:
      - bundle:/usr/local/bundle
      - .:/app
    working_dir: /app/spec
    networks:
      - network
    command: bash -c "bundle install && rspec *"
    depends_on:
      - test_database

volumes:
  pgdata:
  test_pgdata:
  bundle:

networks:
  network: